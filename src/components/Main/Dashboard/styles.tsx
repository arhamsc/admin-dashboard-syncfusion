import { SxProps } from '@mui/material';

export const cardStyles: SxProps = {
    position: 'relative',
    width: '20%',
    zIndex: -1,
};

export const cardMediaStyles: SxProps = {
    position: 'absolute',
    top: 0,
    right: 0,
    height: '100%',
    width: '100%',
    zIndex: -1,
};
